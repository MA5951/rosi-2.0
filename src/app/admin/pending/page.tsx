"use client"

import ArticlePage from '@/app/admin/components/pendingPage'; 

const Home: React.FC = () => {

  return (
    <main>
      <ArticlePage pageTitle='pending'/>
    </main>
  );
}

export default Home;
